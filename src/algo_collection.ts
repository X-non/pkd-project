import { type SquareMatrix, CompleteGraph, type Edge } from "./graph";

type Path<T> = {items: Array<T>};


/*
using memoization

*/

function add_mock<T>(graph: CompleteGraph<T>): CompleteGraph<T>{
    graph.weight_matrix
}

function Held_Karp<T>(graph: CompleteGraph<T>): Path<T> {

}